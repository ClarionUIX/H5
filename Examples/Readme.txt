For all the examples, make sure you adjust your output path. By default, the output for the examples is \AppBroker\Exec\ProgramName\


Inventory

Make sure you copy the TPS files and the "images" folder to the output folder. It contains example data.

If running the app for the first time, a default user is created, login: admin Password: admin 

After that, you can add users and modify rights on the Web Users section of the program (visible only under H5).

Please get a valid Google Maps API Key, without one, the Google Maps integration section will not work. After getting one valid Google Maps API key, replace the content of the global var at Main / Open Window / Embed.
